[phases.setup]
nixPkgs = ["nodejs_18", "npm"]

[phases.install]
cmds = ["npm install", "cd server && npm install"]

[phases.build]
cmds = ["cd server && npm run build", "npm run build"]

[start]
cmd = "cd server && npm start"

[deploy]
healthcheckPath = "/api/health"
healthcheckTimeout = 100